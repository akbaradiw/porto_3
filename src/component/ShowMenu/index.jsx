import React from "react"
import { useSelector, useDispatch } from "react-redux"
// import {getMenu, menuSelector } from "../../redux/features/menu/menuSlice"
import { useEffect } from "react"
const ShowMenu = () => {
// const dispatch = useDispatch()
// const menu = useSelector(menuSelector.selectAll)
//  useEffect(() => {
//     dispatch(getMenu())
//  }, [dispatch])

    return (
        <div>
            {/* <h1>from</h1>
            <div>
                {menu.map((item) => (
                    <div>
                        <p>{item.food}</p>
                        <p>{item.drink}</p>
                    </div>
                ))}
            </div> */}
     

        </div>
    )
}
export default ShowMenu